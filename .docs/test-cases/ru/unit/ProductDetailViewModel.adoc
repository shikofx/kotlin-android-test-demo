= Тест-кейсы для класса `ProductDetailViewModel`

Этот документ описывает тест-кейсы для публичных методов класса `ProductDetailViewModel.java`.

'''

== `ProductDetailViewModel(AppDao, AppExecutors, String id)` (Конструктор)

=== Цель
Проверить, что ViewModel корректно запрашивает один продукт на основе ID, переданного при его инициализации.

[cols="1,3,3,3,2"]
|===
| ID | Сценарий | Входные данные / Моки | Ожидаемый результат | Тип теста

| *PDV-1* | Happy Path (получение продукта по ID) | 1. ID продукта (например, "123") передается в конструктор. +
2. `mockAppDao.getProduct(123)` настроен на возврат конкретного `ProductModel`. | 1. `appDao.getProduct()` вызывается с корректным целочисленным ID (123). +
2. LiveData `viewModel.product` обновляется продуктом, возвращенным из DAO. | Позитивный
|===